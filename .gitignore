*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build  -- intentionally committed; Hostinger shared has no Node, so
# we ship pre-built Vite assets through git. Regenerate before every commit
# that changes resources/css/**, resources/js/**, or any blade file whose
# Tailwind class usage changes, via `npm run build`.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
/database/*.sqlite
/database/*.sqlite-*
Homestead.json
Homestead.yaml
Thumbs.db
